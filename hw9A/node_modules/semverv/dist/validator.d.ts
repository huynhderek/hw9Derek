export declare abstract class Validator {
    config: any;
    constructor(config: any);
    abstract validate(hashable: any, config: any): boolean;
    valid(hashable: any, config: any): boolean;
    r(p: number): boolean;
}
